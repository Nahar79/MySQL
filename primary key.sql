-- create table using primary key
create table voter(voterid varchar(30) primary key,
votername varchar(100),
voterdob date,
voterconst varchar(100),
voteraddr varchar(100)
);

--  show table description
desc voter;

-- add primary key for existing table









